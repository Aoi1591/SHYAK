on: 
  push:
    branches:
      - main
      - sub

name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{Repository secrets.FTP_SERVER}}
        username: ${{Repository secrets.FTP_USERNAME}}
        password: ${{Repository secrets.FTP_PASSWORD}}
